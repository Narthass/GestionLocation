{% extends 'base.html.twig' %}

{% block title %}{{ formName }} - {{ parent() }}{% endblock %}

{% block body %}
  <!-- The Form Section -->
  <div class="w3-container w3-content w3-center w3-padding-64" style="max-width:800px" id="form">
      <h1>{{ formName }}</h1>
      <div>
        {{ form_row(form.SommePayee) }}
      </div>
  </div>
{% endblock %}